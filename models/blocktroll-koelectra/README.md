# BlockTroll model output

This directory is populated by the Windows training workflow.

Keep generated weights, tokenizer files, and model configs out of Git. Copy or
sync a verified model here only when the Mac development server needs to test
`ENABLE_MODEL=true`.
